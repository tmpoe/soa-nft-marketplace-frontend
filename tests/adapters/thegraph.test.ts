import { assert, describe, test } from "matchstick-as/assembly/index"

describe("NftMarketplaceEventDB", () => {
    test("Should get latest nft for user", () => {
        assert.assertTrue(true == true)
    })
})
